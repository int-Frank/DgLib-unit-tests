#include "TestHarness.h"
#include "DgBoundedSND.h"

#include <fstream>
TEST(Stack_BoundedSND, creation_BoundedSND)
{
  /*std::ofstream ofs("values.txt");
  size_t nValues = 1000;
  double values[1000] = {};
  Dg::GetSNDValues(0.0, 1.0, -2.0, 2.0, nValues, values);

  for (size_t i = 0; i < nValues - 1; i++)
  {
    double diff = abs(values[i] - values[i+1]);
    ofs << diff << "\n";
  }*/
}